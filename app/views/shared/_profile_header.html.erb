<div class="wrapper">
  <div class="container text-center">
    <div class="row">
      <div class="col-xs-8 col-xs-offset-2">
          <% avatar_url = current_user.facebook_picture_url || "http://placehold.it/30x30" %>
          <%= image_tag avatar_url, class: "avatar" %>
          <h1>Hello <%= current_user.first_name %> <%= current_user.last_name %></h1>
          <h5>You are a taxidermist since <%= @user.created_at.strftime("%B %d %Y") %></h5>
        <div class="tabs">
          <%= link_to 'Reviews', profile_path, class: "tab active" %>
          <%= link_to 'Bookings', profile_bookings_path, class: "tab" %>
          <%= link_to 'Animals', profile_animals_path, class: "tab" %>
        </div>
      </div>
    </div>
  </div>
</div>
